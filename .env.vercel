# AgentHub Vercel 部署指南

## 🎯 架构说明

AgentHub 需要部署三个部分：

1. **前端应用** → Vercel（免费托管）
2. **后端 API** → Railway 或 Render（Node.js 服务器）
3. **数据库** → Railway Postgres 或 Turso（免费层）

---

## 🚀 第一步：部署后端 API（推荐 Railway）

### 1.1 创建 Railway 账号
- 访问 [railway.app](https://railway.app)
- 使用 GitHub 登录
- 创建新项目

### 1.2 部署后端

```bash
# 1. 在 Railway 中添加新项目
# 2. 连接 GitHub 仓库
# 3. 选择后端服务（Node.js）

# Railway 会自动检测并部署
```

### 1.3 配置环境变量

在 Railway 控制台添加以下环境变量：

```
NODE_ENV=production
PORT=3001
DATABASE_URL=postgresql://user:password@host:5432/agenthub
JWT_SECRET=your-super-secret-key-at-least-32-characters
JWT_EXPIRES_IN=7d
CORS_ORIGIN=https://your-frontend.vercel.app
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
PLATFORM_FEE_PERCENT=10
```

### 1.4 获取后端 API 地址

部署完成后，你会获得一个 URL，例如：
```
https://agenthub-api.up.railway.app
```

---

## 🚀 第二步：部署前端（Vercel）

### 2.1 准备 Vercel 配置文件

项目已包含 Vercel 配置，现在创建环境变量文件：
<minimax:tool_call>
<invoke name="Write">
<parameter name="content"># ==============================
# Vercel 前端环境变量
# ==============================

# 后端 API 地址（Railway 部署后填入）
VITE_API_URL=https://your-railway-api.up.railway.app

# 可选：Vercel 域名
VITE_VERCEL_URL=https://your-project.vercel.app
